# {Topic} Knowledge Graph

Reference: none

## Nodes

### {Concept Name}
- Status: gap
- Depth: none | recall | apply | explain
- Prerequisites: []
- Cold quiz pending: no
- Last quiz format: none
- Quiz history: []

<!-- Depth scale (Bloom's Taxonomy): recall = can repeat | apply = can use in context | explain = can teach others -->
<!-- Quiz history convention: each entry uses prefix `warm:` or `cold:` followed by outcome.
     warm: passed (no hint) | warm: passed (hint used) | warm: failed
     cold: passed (no hint) | cold: passed (hint used) | cold: failed
     warm = same-session retrieval (Phase 4) — caps at gap→partial only.
     cold = next-session retrieval (Phase 2.0) — gates partial→understood. -->
<!-- Backward-compat: nodes missing `Cold quiz pending` or `Last quiz format` lines are
     treated as default (`no` / `none`) at read time. Phase 5 archive expands such nodes
     to the 6-line form when they are next updated (gradual migration, no batch rewrite). -->
